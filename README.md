Здравствуйте это end-to-end CV Pipeline, готов пояснить каждую строку кода
Написал детекцию клеток крови (BCCD) от данных до продакшн-сервинга, поднял в тритоне(трайтон) обучение YOLO11,
оптимизация в ONNX потом TRT с замером скоростей и потерь точности, развёртывание в Triton Inference Server,
FastAPI как точка входа с поинтами для картинок + видео с трекингом и мониторинг Prometheus + Grafana.
Весь стек поднимается одной командой docker compose up -d + из корня uvicorn api:app --reload --port 8080
препроцесс/постпроцесс — на клиенте, инференс — в Triton, трекинг — в API.
Резы:
mAP@0.5  0.916
mAP@0.5–0.95 0.64
Инференс .pt TRT FP16 10.9 ms против 3.7 ms почти 3×
Потеря mAP от FP16 0.02 3%
Per-class пороги по F1-кривой: Platelets 0.444, RBC 0.304, WBC 0.392
